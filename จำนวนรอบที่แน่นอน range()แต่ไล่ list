inputRound = int(input("Please Enter Number : "))
print(list(range(inputRound)))
for x in range(inputRound):
    inputNumber = int(input("x"+str(x+1)+":"))

#list เป็นการไล่ ตั้งแต่ 0 ถืงตัวนั้นๆที่เราใส่
#"list" หรือลิสต์ เป็นโครงสร้างข้อมูลที่ใช้เก็บข้อมูลรายการโดยไม่จำกัดขนาด คุณสามารถเพิ่มหรือลดรายการได้ตามต้องการโดยไม่ต้องรู้ขนาดล่วงหน้า มันเป็นโครงสร้างข้อมูลที่ใช้งานกับข้อมูลรายการที่เปลี่ยนไปตลอดเวลา.