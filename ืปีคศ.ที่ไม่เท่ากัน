# รับปีจากผู้ใช้
year = int(input("ป้อนปี (ค.ศ.): "))

# ตรวจสอบว่าเป็นปีอธิกสุรทินหรือไม่
if (year % 4 == 0 and year % 100 != 0) or year % 400 == 0:
    print("Leap Year")
else:
    print("Not a Leap Year")
