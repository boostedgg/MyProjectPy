import math              #ใส่ไว้ด้านบนเสมอเปรียบเสมือนหัวข้อหลัก
print(math.ceil(10.59))  #library ที่เรา import เข้ามา เราสามารถเข้าไปดูโค้ดแต่ละฟังก์ชันด้วยการกดปุ่ม Ctrl ใน Windows หรือ command ใน Mac ค้างไว้แล้วคลิกที่ฟังก์ชันที่เราอยากดู