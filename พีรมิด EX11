height = 5

for i in range(height):
    # สร้างช่องว่างด้านหน้าในแต่ละบรรทัด
    for j in range(height - i - 1):
        print(" ", end="")

    # สร้างดาว (*) ในแต่ละบรรทัด
    for k in range((2 * i) + 1):
        print("*", end="")

    # ขึ้นบรรทัดใหม่หลังจากแต่ละบรรทัด
    print()